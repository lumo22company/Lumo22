---
description: Redirect — Git always (commit + push); see railway-deploy.mdc
alwaysApply: false
---

# Git and Railway

Canonical rule: **`.cursor/rules/railway-deploy.mdc`** (`alwaysApply: true`): **always commit with Git**; push to deploy. Do not duplicate deploy steps here.
